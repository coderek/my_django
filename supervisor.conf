[program:gunicorn]
command=gunicorn -b 127.0.0:8000 my_django.wsgi
directory=/apps/my_django
user=root
autorestart=true
redirect_stderr=true
environment=django_env="prod"

[supervisord]
pidfile=supervisord.pid
logfile=supervisord.log

[supervisorctl]

[group:app]
programs=gunicorn